# Bet tracking

This folder stores the daily pick log and win/loss tallies.

- `picks.jsonl`: one JSON object per posted card
- `results.jsonl`: one JSON object per settled game/leg + computed outcomes
- `summaries.jsonl`: daily/weekly/monthly rollups posted to Discord
