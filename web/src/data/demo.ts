import type { DailyCard } from './sample';

// Empty demo data — production reads live Supabase data via usePicks hook.
// Demo mode (?demo=1) is preserved as an escape hatch but shows empty data.
export const demoCards: DailyCard[] = [];
